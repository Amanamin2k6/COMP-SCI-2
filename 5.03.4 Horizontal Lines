function start(){
    // calling horizontalLine function with parameters: length, yposition
    horizontalLine(100, 200);
    horizontalLine(200, 100);   
    horizontalLine(300, 20);
}
function horizontalLine(length, y){
    var line = new Line(0, length, y, length);
    add(line);
}
